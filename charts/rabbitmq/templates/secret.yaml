apiVersion: v1
kind: Secret
metadata:
  name: {{ include "rabbitmq.fullname" . }}-secret
  labels:
    {{- include "rabbitmq.labels" . | nindent 4 }}
type: Opaque
stringData:
  rabbitmq-password: "guest"
  erlang-cookie: "erlangcookiesecret"
  rabbitmq-username: "guest"