apiVersion: v1

kind: Service
metadata:
  name: {{ include "adder-ms.fullname" . }}
  labels:
    {{- include "adder-ms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "adder-ms.selectorLabels" . | nindent 4 }}