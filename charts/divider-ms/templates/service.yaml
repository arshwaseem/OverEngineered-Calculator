apiVersion: v1

kind: Service
metadata:
  name: {{ include "divider-ms.fullname" . }}
  labels:
    {{- include "divider-ms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "divider-ms.selectorLabels" . | nindent 4 }}