apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-ms.fullname" . }}
  labels:
    {{- include "user-ms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-ms.selectorLabels" . | nindent 4 }}