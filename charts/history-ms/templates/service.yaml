apiVersion: v1
kind: Service
metadata:
  name: {{ include "history-ms.fullname" . }}
  labels:
    {{- include "history-ms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "history-ms.selectorLabels" . | nindent 4 }}