CREATE TABLE IF NOT EXISTS history (id BIGSERIAL PRIMARY KEY, serviceName VARCHAR(50) NOT NULL, numA DOUBLE PRECISION NOT NULL, numB DOUBLE PRECISION NOT NULL, result DOUBLE PRECISION NOT NULL ,timeStamp TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP)